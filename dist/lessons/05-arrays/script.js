"use strict";
const nums = [1, 2, 3, 4, 5, 6];
const strs = [];
strs.push("test");
console.log(str);
const cars = [];
cars.push({ brand: "BWM", wheels: 4 });
// запушить получится только объект с интерфесом Car
// array in array
const arrOfArr = [["sd"], ["sd"]];
arrOfArr.push(["AA"]);
function printArr(arr) {
    arr.forEach((item, id, array) => {
        console.log(item, id, array);
    });
}
